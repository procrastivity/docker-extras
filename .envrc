# shellcheck shell=bash
# Load the dev shell when nix is available; degrade gracefully when not.
if ! has nix; then
  echo "docker-extras: nix not found; bring shellcheck/git-cliff/gh/pre-commit yourself (or install nix: https://install.determinate.systems)" >&2
  return 0
fi
use flake
